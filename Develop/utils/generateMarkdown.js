// function to generate markdown for README
function generateMarkdown(data) {
  return `# ${data.title}`;
  return `# ${data.description}`;
  return `# ${data.instillation}`;
  return `# ${data.userstory}`;
  return `# ${data.contribution}`;
  return `# ${data.test}`;

}
module.exports = generateMarkdown;
